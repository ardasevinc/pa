                   pa
        a simple password manager
         https://passwordass.org

  features
    - encryption implemented using age[1]
    - automatic key generation
    - automatic git tracking
    - multiple identity/recipient support
    - written in portable posix shell
    - simple to extend
    - only ~200 lines of code
    - pronounced "pah" - as in "papa"


  dependencies
    - age
    - age-keygen
    - git (optional)
    - Go 1.26+ (only to build pa-xfer)


  usage
    pa
      a simple password manager

    commands:
      [a]dd  [name] - Add a password entry.
      [d]el  [name] - Delete a password entry.
      [e]dit [name] - Edit a password entry with vi.
      [g]it  [cmd]  - Run git command in the password dir.
      [l]ist [cat]  - List all entries in a category.
      [s]how [name] - Show password for an entry.
      [m]ove [src] [name] - Rename a password entry.

    env vars:
      data directory:   export PA_DIR=~/.local/share/pa
      password length:  export PA_LENGTH=50
      password pattern: export PA_PATTERN=A-Za-z0-9_-
      disable tracking: export PA_NOGIT=


  safe transfer
    pa-xfer moves entries between pa stores while each machine keeps its own
    age identity. Transfers are a set union by name: existing destination
    names are always skipped, even when the two values differ. There is no
    overwrite mode.

    On the destination machine, obtain its public recipient file or inspect
    its fingerprint:

      $ pa-xfer recipient

    On the source machine, encrypt a binary-safe bundle directly to that
    recipient:

      $ pa-xfer export --to ./destination-recipients ./to-destination.age

    Back on the destination, verify and import it:

      $ pa-xfer verify ./to-destination.age
      $ pa-xfer import ./to-destination.age

    Use --json on any pa-xfer command for machine-readable results. Import
    requires a clean password Git repository when Git tracking is enabled.
    Successful imports create private before/after snapshots and a receipt
    under $PA_DIR/backups/<transaction-id>/.

    The pa-export and pa-import commands are compatibility entry points for
    pa-xfer. The old tab-delimited passphrase format is intentionally not
    supported because it could not preserve arbitrary bytes and wrote all
    plaintext values to a temporary file.

    See docs/transfer-protocol.md for the format and security invariants.

    For direct SSH synchronization, save the peer's trust record once. The SSH
    destination defaults to the peer name, so an OpenSSH config alias gives the
    shortest setup:

      $ pa-xfer peer add devbox

    Pairing fetches the current recipient fingerprint, tells you how to verify
    it on the other machine, and requires the exact phrase "add devbox". For an
    independently obtained pin or noninteractive setup, supply it explicitly:

      $ pa-xfer peer add devbox --fingerprint sha256:...

    Daily synchronization is then:

      $ pa-xfer sync devbox

    Inspect or manage saved trust without contacting the peer:

      $ pa-xfer peer list
      $ pa-xfer peer show devbox
      $ pa-xfer peer replace devbox
      $ pa-xfer peer remove devbox

    Replace is the only command that can change a saved host or fingerprint. It
    fetches the proposed peer first and requires another exact confirmation or
    an explicit matching --fingerprint. Sync itself never prompts, accepts, or
    stores a changed fingerprint. Noninteractive removal requires --yes.

    Peer records live under $PA_DIR/peers with private filesystem permissions.
    They contain only the peer name, SSH destination, and public recipient-file
    fingerprint. Recipient files, SSH executables, and SSH options are not
    cached. Put persistent ports, keys, and jump-host settings in ~/.ssh/config.

    Sync is an explicit push followed by a pull. Both directions preserve
    existing names, so divergent same-name values remain local to each machine.
    Only age-encrypted bundles cross SSH. The remote command is always exactly
    "pa-xfer serve", SSH host-key checking is required, and SSH runs in batch
    mode.

    The original low-level surface remains available for one-off and recovery
    use. It never reads or writes the peer registry:

      $ pa-xfer peer probe --host user@example
      $ pa-xfer sync --host user@example \
          --peer-fingerprint sha256:...

    The old `pa-xfer peer --host ...` spelling remains a compatibility alias.
    Low-level commands still accept repeated --ssh-option flags.

    Imports and restores retain recovery material under $PA_DIR/backups. List
    it with:

      $ pa-xfer backups

    Restoring is the only operation allowed to replace existing entry names.
    It requires an explicit confirmation flag and restores the exact encrypted
    entry set from one snapshot:

      $ pa-xfer restore --transaction TRANSACTION_ID --phase before --yes

    Restore verifies every backup entry against the machine's current local
    age identity before touching the live store. It then replaces or displaces
    ciphertext one entry at a time, verifies exact ciphertext equality, creates
    its own before/after snapshots and receipt, and records one Git commit. A
    crash can leave a valid mixture of old and restored entries, never a
    plaintext file or an absent password directory. After reviewing the dirty
    Git state, rerun with --allow-dirty to converge. The snapshot's Git database
    is retained as recovery evidence but is not installed over the current
    repository history.


  installation
    $ make check
    $ make install PREFIX="$HOME/.local"


  command examples
    $ pa add test
    generate a password? [y/N]: y
    saved 'test' to the store.

    $ pa list
    test

    $ pa show test
    vJwKuEBtxBVvdR-xppTdfofIei0oLlkoSK4OCSP2bMEBsP6ahM

    $ pa edit test
    <opens $EDITOR or vi>

    $ pa mv test example

    $ pa del example
    delete password 'example'? [y/N]: y

    $ pa git log --oneline
    bbe85dc (HEAD -> main) delete 'example'
    c7f76d7 move 'test' to 'example'
    b597c04 edit 'test'
    cba20cc add 'test'
    ef76f7e initial commit


  faq
    > how does this differ from pass, passage, etc?

      pa is smaller. simpler. cleaner. plainer.
      harder. better. faster. stronger.
      more than ever, hour after hour
      work is never over

    > is pa secure?

      if you would like to understand the
      security characteristics of pa, please
      read my blog post[2], and my explanation[3].

    > why u make this?

      see [2].

    > where are my keys?

      probably the default locations:
        ~/.local/share/pa/identities
        ~/.local/share/pa/recipients

    > where are my passwords?

      probably the default location:
        ~/.local/share/pa/passwords


  credits
    - pa was originally forked from pash[4] by dylanaraps[5]
    - age[1] is a project by Filippo Valsorda[6]


  refs
    [1]: https://age-encryption.org
    [2]: https://j3s.sh/thought/storing-passwords-with-age.html
    [3]: https://github.com/biox/pa/issues/10#issuecomment-1369225383
    [4]: https://github.com/dylanaraps/pash
    [5]: https://github.com/dylanaraps
    [6]: https://filippo.io
